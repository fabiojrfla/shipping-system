<h2>Transportadoras</h2>
<% if @shipping_companies.any? %>
  <table>
    <thead>
      <tr>
        <th>CNPJ</th>
        <th>Nome Fantasia</th>
        <th>Localização</th>
      </tr>
    </thead>
    <tbody>
      <% @shipping_companies.each do |shipping_company| %>
      <tr>
        <td><%= shipping_company.registration_number %></td>
        <td><%= shipping_company.brand_name %></td>
        <td><%= shipping_company.city %>/<%= shipping_company.state %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Não existem transportadoras cadastradas</p>
<% end %>
<div>
  <%= link_to 'Nova Transportadora', new_shipping_company_path %>
</div>
